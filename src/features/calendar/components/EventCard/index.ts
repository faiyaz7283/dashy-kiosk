/**
 * EventCard component barrel export.
 *
 * Shared event card for calendar views with size variants and optional time display.
 */

export { EventCard, type EventCardProps, type EventCardSize } from './EventCard'
