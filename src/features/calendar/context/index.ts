/**
 * Calendar context barrel export.
 *
 * Provides CalendarDataProvider and useCalendarContext for consuming
 * cached calendar data across all calendar views.
 */

export { CalendarDataProvider, useCalendarContext } from './CalendarDataContext'
export type { CalendarDataContextValue, CalendarDataProviderProps } from './CalendarDataContext'
